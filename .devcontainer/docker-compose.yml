services:
  app:
    container_name: sbe-app
    build:
      context: .
      dockerfile: Dockerfile
    deploy:
      resources:
        limits:
          cpus: "4.0"
          memory: 2048M
        reservations:
          cpus: "0.5"
          memory: 2048M
    command: sleep infinity
    restart: unless-stopped