# Scalar REST Controller Example

A simple Spring Boot application which demonstrates a complete REST API with interactive documentation via Scalar UI.

## Running the Application

```bash
./gradlew :projects:scalar-webmvc:bootRun
```

Once the server is running, you can access the Scalar UI at [http://localhost:8080/scalar](http://localhost:8080/scalar) to explore and test the REST endpoints.
