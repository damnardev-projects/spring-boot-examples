spring:
  main:
    banner-mode: off
  application:
    name: jpa-example
  datasource:
    url: jdbc:h2:mem:employee-db
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false