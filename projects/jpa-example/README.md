# JPA Example

A Spring Boot application which starts a JPA context with an in-memory H2 database and executes an `ApplicationRunner`.

## Running the Application

```bash
./gradlew :projects:jpa-example:bootRun
```