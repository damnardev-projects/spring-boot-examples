# Spring Value Annotation

A simple Spring Boot application which demonstrates the use of the `@Value` annotation to inject properties.

## Running the Application

```bash
./gradlew :projects:value-annotation:bootRun
```
