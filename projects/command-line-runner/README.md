# Spring Command Line Runner

A simple Spring Boot application which starts the context and executes a `CommandLineRunner` to print messages.

## Running the Application

```bash
# Run the application
./gradlew :projects:command-line-runner:bootRun

# Run with arguments
./gradlew :projects:command-line-runner:bootRun --args="Foo Bar"
```
