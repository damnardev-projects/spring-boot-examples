# Spring Configuration Properties

A simple Spring Boot application which demonstrates the use of the `@ConfigurationProperties` annotation to map properties to a Java record instead of using `@Value`.

## Running the Application

```bash
./gradlew :projects:configuration-properties:bootRun
```
