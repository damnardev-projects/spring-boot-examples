# Spring Scheduled Annotation

A simple Spring Boot application which demonstrates the use of the `@Scheduled` annotation to schedule tasks (e.g., fixed rate, fixed delay, cron).

## Running the Application

```bash
./gradlew :projects:scheduled-annotation:bootRun
```
